# Known unmaintained crate advisories in transitive dependencies.
# All are pulled in by staticdatagen and cannot be resolved without
# upstream changes. None represent active security vulnerabilities.

[advisories]
ignore = [
    "RUSTSEC-2024-0320", # yaml-rust — via syntect/comrak (staticdatagen)
    "RUSTSEC-2024-0436", # paste — via dtt (staticdatagen)
    "RUSTSEC-2025-0057", # fxhash — via selectors/scraper (staticdatagen)
    "RUSTSEC-2025-0068", # serde_yml — patched locally, transitive via staticdatagen
    "RUSTSEC-2025-0119", # number_prefix — via indicatif
    "RUSTSEC-2025-0141", # bincode — via syntect (staticdatagen)
]
