[package]
name = "serde_yml"
version = "0.0.12"
edition = "2021"
description = "Local safe YAML serde implementation"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
indexmap = "2"

[lints.rust]
unsafe_code = "forbid"
